# Auto-generated by CISECSL/org-tools bootstrap
# This workflow generates .claude/context.md and syncs it to Notion
name: Update Context

on:
  push:
    branches: [main, dev]
    paths-ignore:
      - '.claude/context.md'
  workflow_dispatch:

jobs:
  update-context:
    uses: CISECSL/org-tools/.github/workflows/generate-context.yml@main
    with:
      repo_name: ${{ github.event.repository.name }}
      repo_description: ${{ github.event.repository.description }}
    secrets:
      NOTION_API_TOKEN: ${{ secrets.NOTION_API_TOKEN }}
      NOTION_CONTEXT_PARENT_PAGE_ID: ${{ secrets.NOTION_CONTEXT_PARENT_PAGE_ID }}
